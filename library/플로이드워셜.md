N개의 정점에서 N개의 정점으로가는 모든 경로의 최단경로를 구할수 있는 알고리즘
- 모든 정점을 확인하여 `min(i->j, i -> k -> j)`의 경로로 갱신한다.

```c++
for int( k=0; k<n; k++) {
    for (int i = 0 ; i < n; i++) {
		for (int j = 0 ; j < n; j++) {
            dp[i][j] = min(dp[i][j], dp[i][k] + dp[k][j]); 
		}
	}
}
```